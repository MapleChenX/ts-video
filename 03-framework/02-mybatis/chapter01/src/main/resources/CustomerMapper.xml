<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="stage01.CustomerMapper">
  <select id="selectAll" resultType="stage01.Customer">
    select * from t_customer;
  </select>

  <select id="selectByUsername" resultType="stage01.Customer" parameterType="String">
    select * from t_customer where username like concat('%', #{value}, '%');
  </select>

  <insert id="addCustomer" parameterType="stage01.Customer">
    insert into t_customer(username, password, sex, mobilephone, education, jobs)
    values(#{username}, #{password}, #{sex}, #{mobilephone}, #{education}, #{jobs});
  </insert>
</mapper>